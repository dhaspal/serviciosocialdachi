# =============================================================================
# Backend (copia a backend/.env o define en Render → Environment)
# =============================================================================
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=servicio_social
API_PREFIX=/api/v1

# =============================================================================
# Frontend (copia a frontend/.env para desarrollo local)
# =============================================================================

# Desarrollo: URL del API. En Render/producción déjalo vacío (mismo origen).
VITE_API_BASE_URL=http://localhost:8000
VITE_API_PREFIX=/api/v1
